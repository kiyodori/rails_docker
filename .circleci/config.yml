version: 2.1
orbs:
  ruby: circleci/ruby@1.1.2

jobs:
  build:
    docker:
      - image: circleci/ruby:2.7-node
    steps:
      - checkout
      - run cd src
      - ruby/install-deps

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
